/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['skillsphereaccount.blob.core.windows.net'],
    }};

export default nextConfig;
